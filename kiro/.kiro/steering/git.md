# Git

- Do not commit or push changes when performing a task.
- Always prefer `git rebase` over `git merge` when syncing branches.
- When syncing a feature branch with another branch (e.g., develop), use `git rebase origin/<branch>` instead of `git merge`.
